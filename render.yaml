services:
  - type: web
    name: moyaki-trace-backend
    env: node
    rootDir: backend
    buildCommand: pnpm install && npm run check
    startCommand: npm start
    plan: free
    autoDeploy: true
