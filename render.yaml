services:
  - type: web
    name: moyaki-trace-backend
    env: node
    rootDir: backend
    buildCommand: npm install && npm run check
    startCommand: npm start
    plan: free
    autoDeploy: true
